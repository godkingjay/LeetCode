class Solution {
    TreeNode node = null;
    public TreeNode searchBST(TreeNode root, int val) {
        traverse(root, val);
        return node;
    }
    
    public void traverse(TreeNode root, int val){
        if(root == null) return;
    }
}
